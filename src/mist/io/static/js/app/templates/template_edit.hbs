<!-- Popup Wrapper -->
<div id="template-edit-popup"
    data-role="popup"
    data-overlay-theme="b"
    data-transition="slideup">

    <!-- Popup -->

    <div id="template-edit"
         class="mid-popup
            ui-popup
            ui-body-a
            ui-overlay-shadow
            ui-corner-all"
         data-role="popup"
         data-enhanced="true"
         data-transition="flip">

         <!-- Header -->

        <div class="ui-header ui-bar-b ui-caps">
            <h1 class="ui-title">Edit template</h1>

            <a href="javascript:void(0)" class="close" data-rel="back">&#10005;</a>
        </div>

        <!-- Body -->

        <div role="main" class="ui-content" data-theme="a">

            <!-- New cloud name text field -->
            <form {{action "saveClicked" on="submit" target=view}}>
                <div class="form-field {{if Mist.templateEditController.newName 'filled' 'empty'}} required">
                    <label for="template-edit-new-name">Name</label>
                    {{view Mist.TextField
                        id="template-edit-new-name"
                        value=Mist.templateEditController.newName}}
                </div>

                <div class="form-field">
                    <label for="template-edit-new-description">Description (optional)</label>
                    {{view Mist.TextArea
                        id="template-edit-new-description"
                        value=Mist.templateEditController.newDescription}}
                </div>

                {{#if Mist.templatesController.renamingTemplate}}
                    <div class="ajax-loader"></div>
                {{/if}}

                <div class="ok-cancel" data-role="controlgroup" data-type="horizontal" >
                    <button type="submit" id="template-edit-ok"
                        class="ui-btn ui-btn-d ui-caps">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>
